#!/bin/bash

# Проверяем, что скрипт запущен с правами root
if [ "$EUID" -ne 0 ]
  then echo "Пожалуйста, запустите скрипт с правами суперпользователя"
  exit
fi

# Отображаем активные сетевые соединения
lsof -i
